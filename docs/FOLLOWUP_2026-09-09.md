# 2026-09-09 개인 노트북 복구와 GitHub 이전

## 완료한 데이터 복구

- 메인 SQLite·업로드 자료·Chroma 전체를 `.backups/repair-20260909-111813-161845/`에 백업.
- 공고 R26BK01618627의 FTS5 손상을 복사본에서 검증한 뒤 원본에서 재구성.
- 7개 인덱스의 326개 source 메타데이터를 media:// 상대 참조로 변경.
- 복구 후 7개 모두 SQLite 무결성 정상, 참조 누락 0건. 임베딩은 유지.
- 과거 학원 PC 절대 경로도 현재 MEDIA_ROOT로 해석하도록 추출 코드를 개선.
- HWP 변환은 복사된 exe 실행기의 이전 Python 경로 대신 현재 Python으로 패키지 진입점을 호출.

## LLM 연결

- 기능별 OpenAI/Ollama provider와 모델 설정 분리.
- native Ollama 요청에 context 크기·출력량·thinking 설정 명시.
- Pydantic 구조화 출력 검증, 시간 제한, 출력 잘림·입력 한도 검사.
- 클라우드 키를 로컬 서버로 전달하지 않으며 자동 클라우드 fallback 없음.
- 임베딩·사업자등록증·웹 검색은 별도 OpenAI 경로 유지.
- 각 PC의 .env 설정으로 모델 선택. 상위 모델 설치나 다른 프로젝트 설정은 변경하지 않음.

## 검증과 저장소

최종 검증 결과는 이 문서의 아래 기록을 확인합니다. 초기 129개 테스트는 이전 코드 기준이며, 새 회귀 테스트와 실제 연결을 별도로 확인합니다.

## 다음 제품 개발 항목

이번 범위는 PC 이전 장애·GitHub 연결·모델 연결 설정의 안정화입니다. 기존 docs/TODO.md의 모든 제품 기능을 완료했다는 뜻은 아닙니다.

1. 30장 제안서의 필수조건 누락·자리표시자·작은 글씨 검수와 자동 보정.
2. 회사 자료·유사 제안서 적용 전후의 실제 품질 비교.
3. 긴 제안서 생성을 백그라운드 작업으로 분리하고 진행·실패·재시도 상태 제공.
4. 민감 자료의 클라우드 전송을 제한하는 완전 로컬 모드와 별도 임베딩 전환 평가.
5. 모델 상위 버전 비교 평가 및 운영 배포 준비.

이전 문서의 Bid2 명칭과 날짜는 과거 작업 기록으로 보존합니다. 현재 저장소의 시작 안내는 루트 README.md를 기준으로 합니다.

## 실제 연결 확인

- 복구된 공고 문서 3개를 읽음: PDF 5페이지, HWP 전체 문서, PDF 51페이지. 추출 실패 없음.
- native Ollama 구조화 출력에서 한국어 명칭·기간을 정확히 반환.
- 실제 공고 R26BK01637158의 과업 목적을 Qwen14B로 요약하고 출처 4번을 표시하는 답변 생성 성공.
- 최초 벡터 검색은 OpenAI API 잔액 부족(429 credit_balance_exhausted)으로 실패. 모델 목록 조회 성공과 추론·임베딩 이용 가능 여부는 다름.
- API 호출 없는 키워드 검색을 추가하고 같은 실제 공고에서 검색·답변 성공 확인. 기존 임베딩은 보존.
- 개인 노트북의 이 프로젝트만 Qwen14B 채팅 + 키워드 검색으로 설정. 다른 프로젝트·학원 PC 설정과 모델 설치는 변경하지 않음.
- 클라우드 분석·제안서·사업자등록증·웹 검색은 API 잔액 충전 또는 해당 기능의 별도 로컬 전환이 필요함.
- 기존 Django 비밀값은 공개 코드에서 제거하고 제외된 .env에 보존. 비개발 모드는 비밀키 명시를 요구함.

## 최종 검증

- Django 전체 자동 테스트 143개 통과(79.837초).
- Django check, migrate --check, makemigrations --check --dry-run 통과.
- Chroma 7개 재검사: integrity=ok, 변경할 경로 0개, 참조 누락 0개.
- Next.js lint 및 production build 통과(TypeScript 검사 포함).
- 실제 로컬 JSON 추출과 공고 문서 검색·답변·출처 표시 성공.
- Git 업로드 대상 소스·템플릿에서 실제 로컬 키 및 개인 데이터 경로를 검사.
- 최초 빈 저장소에서 main 브랜치로 구성. 학원 노트북의 과거 Git 이력은 포함하지 않음.

공고 시험 갱신: 최대 2페이지(1,998행)를 조회하여 449건 신규 저장, 927건 업데이트. 현재 DB 53,746건. 전체 기간·전체 페이지 갱신을 의미하지 않습니다.
