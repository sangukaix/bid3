# 실제 공고 로컬 생성 검증 — 2026-09-14

## 결과

**실패: 슬라이드 작성 전 장문 요약 단계에서 중단. 최종 PPTX는 생성되지 않았다.**

검증 공고는 R26BK01637158(해남군교육재단 원어민 화상영어 위탁 운영)이다. 이미 마감된 공고이며 실제 제출 목적이 아닌 생성 기능 검증에 사용했다. 기존 저장 제안서는 덮어쓰지 않았다.

- 모든 AI 역할이 Ollama로 설정돼 있는지 검사하고 실행했다.
- 검증 프로세스에서 OpenAI/AsyncOpenAI 클라이언트 생성을 차단했다. 차단 오류는 발생하지 않았다. 이는 이번 실행 경로의 결과이며 모든 기능의 외부 통신을 감사했다는 뜻은 아니다.
- 공고 원문 57개 위치, 15개 묶음에서 요구사항 199개 추출 완료.
- 회사 지식과 프로젝트 참고자료 컨텍스트 불러오기 완료.
- 최종 중단: local_context.summarize_evidence의 “로컬 문맥 요약이 충분히 줄지 않았습니다.”
- 슬라이드 생성, 회사 주장 대조, PPTX 적용/렌더링 단계는 도달하지 못했다. 결과물 품질 및 OpenAI 대비 품질은 판단할 수 없다.

## 발견한 오류와 변경

1. 요구사항 문장을 240자보다 길게 생성해 Pydantic 검증 오류가 났다.
   - 필드 길이를 프롬프트에 명시했다.
   - 스키마 오류일 때 전체 원문을 유지하고 필드 오류를 안내해 1회 재시도한다.
   - 실제 재실행에서 오류 묶음을 넘어 전체 15개 묶음이 완료됐다.
2. 장문 요약이 출력 한도에 걸리면 전체 생성이 중단됐다.
   - 출력 한도를 별도 예외로 구분한다.
   - 잘린 텍스트를 사용하지 않고 호출자의 설정 한도 안에서 1회 재시도한다.
   - 성공한 개별 요약을 JSON으로 저장해 재실행 때 재사용한다.
   - 재시도도 실패한 긴 원문은 최대 3단계로 분할한다.
   - 실제 실행에서 1,200토큰 한도 후 1,266토큰의 완전한 응답으로 복구된 사례를 확인했다.
   - **분할 후에도 요약이 충분히 줄지 않는 사례가 남아 있다.**
3. 전략 양식의 제한 없는 목록이 로컬 출력 한도 4,096토큰을 초과했다.
   - 로컬용 전략 개요 스키마를 분리했다. 핵심 목록/준수항목은 최대 3개, 목차는 최대 8개, 문장은 180자 이내다.
   - 전체 요구사항 목록은 별도로 유지한다. 전략 개요를 전체 준수 검토로 해석하면 안 된다.
   - 변경 후 실행은 앞단 장문 요약에서 중단됐으므로 **전략 생성 성공은 아직 검증되지 않았다.**

## 다음 검증에서 우선 해결할 부분

- 문맥 전체를 여러 번 압축하는 구조를 검토하고, 요약 실패 시에도 근거/누락 범위를 명시할 수 있는 입력 구성을 설계한다.
- 페이지마다 달라지는 입력 예산 때문에 같은 자료 요약이 반복되는지 측정한다.
- 전략과 슬라이드 생성의 단계별 재개/진행률을 제품에 연결한다.
- 완성된 출력에서 250명(청소년 180/성인 70), 15주, 30회·회당30분·주2회, 1억원, 홈페이지7일 이내, 동시접속150명, 티칭/코칭센터 및 시스템 평가 조건을 원문과 대조한다.
- 회사 실적·강사 수·인증·자격 주장은 업로드 원문과 대조한다. 현재 프로필/옛 제안서만으로 참가자격 충족을 확정하면 안 된다.

## 검증 자료

개인 자료와 실행 로그는 Git 제외 경로 `.local/real-bid-R26BK01637158/`에 보관한다.
최종 실행 로그는 `run-split-retry.log`, 상태는 `status.json`이다.
실제 수정의 회귀 테스트는 최종 테스트 로그로 별도 기록한다.

전체 Django 회귀 테스트: **180개 통과**(40.131초). 실제 공고의 전체 생성 실패와는 별개의 결과다.
